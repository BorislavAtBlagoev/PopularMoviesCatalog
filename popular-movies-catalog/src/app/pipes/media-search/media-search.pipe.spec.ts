import { MediaSearchPipe } from './media-search.pipe';

describe('MediaSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
